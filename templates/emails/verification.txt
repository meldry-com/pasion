{%- if browser_session is defined -%}
  {%- set username = browser_session.user.username -%}
{%- elif user_registration is defined -%}
  {%- set username = user_registration.username -%}
{%- endif -%}

Hello {{ username|default("user") }},

Your email verification code for {{ branding.server_name }} is: {{ authentication_code.code }}

Matrix homeserver: {{ branding.server_name }}
